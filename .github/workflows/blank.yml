name: CI
on:
  push:
   branches: [ "main" ]
  pull_request:
   branches: [ "main" ]
  workflow_dispatch:
jobs:
   build123:
    runs-on: ubuntu-latest
    steps:
     -uses: actions/checkout@v4
     -name: Run a one-line script
     -run: echo Hellow, World!
     -name: Run a multi-line script
     run:
      echo Add other action to bulid,
      echo test, and deploy your project.
      


 
